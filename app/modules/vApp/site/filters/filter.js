'use strict';

/**
 * @ngdoc filter
 * @name vApp.filter:Filter
 * @function
 * @description
 * # Filter
 * Filter in the vApp.
 */
angular.module('vApp').filter('Filter', function () {
    return true;    
});
